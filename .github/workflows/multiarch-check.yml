name: multiarch-check

# Verify the headless KLayout bundle BUILDS + smokes on BOTH linux/amd64 and
# linux/arm64 (native GitHub-hosted runners — free on a public repo). No publish.
# Run this before wiring arm64 into release.yml.

on:
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-24.04
            arch: x86_64
          - runner: ubuntu-24.04-arm
            arch: aarch64
    runs-on: ${{ matrix.runner }}
    name: build-${{ matrix.arch }}
    steps:
      - uses: actions/checkout@v4

      - name: Confirm runner arch
        run: uname -m

      - name: Read pin from upstream.yaml
        id: pin
        run: |
          COMMIT=$(grep -E '^[[:space:]]*commit:' upstream.yaml | head -1 \
            | sed -E 's/.*commit:[[:space:]]*//; s/[[:space:]]*#.*//; s/"//g')
          echo "commit=$COMMIT" >> "$GITHUB_OUTPUT"
          echo "short=${COMMIT:0:12}" >> "$GITHUB_OUTPUT"

      - name: Install Qt-free build deps
        run: |
          sudo apt-get update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y -qq \
            build-essential python3 python3-dev python3-pip python3-tomli \
            zlib1g-dev libexpat1-dev libpng-dev libcurl4-openssl-dev

      - name: Clone KLayout at pinned commit
        run: |
          git clone https://github.com/KLayout/klayout.git /tmp/klayout
          git -C /tmp/klayout checkout "${{ steps.pin.outputs.commit }}"

      - name: Build bundle + tarball (arch from uname -m)
        env:
          KL_COMMIT: ${{ steps.pin.outputs.commit }}
          VERSION: ci-${{ matrix.arch }}
          OUT_DIR: ${{ github.workspace }}/dist
          KL_TREE: /tmp/klayout
        run: bash scripts/build-bundle.sh

      - name: Smoke (import klayout.db on ${{ matrix.arch }})
        run: bash scripts/smoke.sh "dist/vyges-klayout-ci-${{ matrix.arch }}-g${{ steps.pin.outputs.short }}"

      - name: Result
        run: |
          echo "### ${{ matrix.arch }} build OK" >> "$GITHUB_STEP_SUMMARY"
          ls -la dist/*.tar.gz >> "$GITHUB_STEP_SUMMARY"
